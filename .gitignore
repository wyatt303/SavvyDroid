.gradle/
build/
*/build/
.idea/
local.properties
*.iml
.DS_Store
/captures/

# Created under firmware/ because builds run with HOME=/workspace/firmware
# (see firmware/README.md) so PlatformIO's ~/.platformio resolves there.
/firmware/.platformio/
/firmware/.cache/

